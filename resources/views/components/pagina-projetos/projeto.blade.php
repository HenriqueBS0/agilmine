<div class="card">
    <div class="card-body">
        <h5 class="card-title">{{ $titulo }}</h5>
        <p class="card-text">{{ $slot }}</p>
        <a href="{{ $href }}" class="btn btn-primary">Acessar</a>
    </div>
</div>
